const Validations = require('../public');

console.log(Validations);
/*import chai, {expect} from 'chai';

describe('Validation Methods', () => {

  let validationInstance;
  beforeEach('Create a new validation instance', () => {
      validationInstance = new Validations()
  });

  it('creates a new instance of the function', () => {
      const isFunction = typeof validationInstance;
      expect(isFunction).to.be.equal('function');
  });
});*/
